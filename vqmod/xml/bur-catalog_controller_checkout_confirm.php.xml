<modification>
	<id>VQMOD - burro: opencart ispconfig integration</id>
	<version>1.5.5.1</version>
	<vqmver>2.3.2</vqmver>
	<author>Federico Carrara</author>



	<!-- just before a cart is confirmed from the customer, it is saved to db (till now cart was only stored in sessions) -->
	<!-- so when save cart to db, must also save hostings -->
	<file name="catalog/controller/checkout/confirm.php">
		<operation error="abort" >
			<search position="replace"><![CDATA[$this->session->data['order_id'] = $this->model_checkout_order->addOrder($data);]]></search>
			<add><![CDATA[$this->session->data['order_id'] = $this->model_checkout_order->addOrder($data);
			//save hostings of current cart to db (reading from sessions)
			//$this->log->write("checkout/confirm: provo a caricare il modulo");
			$this->load->model('burro/hosting');
			//$this->log->write("checkout/confirm: provo a chiamare saveHostings del modulo");
			$this->model_burro_hosting->saveHostings($this->session->data['order_id']);
			//$this->log->write("checkout/confirm: finito di chiamare saveHostings del modulo!!!!!");
			
			]]></add>
		</operation>
	</file>



</modification>	